"use client";

import ContextStrip from "@/components/layout/ContextStrip";

export default function StudioTipsStrip() {
  return (
    <ContextStrip
      title="Edith Studio"
      subtitle="Tip: Rotate, open drawers, and switch finishes in real-time."
    />
  );
}
